# Example 1 — Acacia tortilis crowns from UAV RGB (binary)

The reference dataset of this repository (`docs/02_data_preparation.md`).
The `_base_/dataset.py` here is the generic template pointed at the acacia
tiles; four model configs share it so that models can be compared under
identical data, augmentation and schedule.
